void print_number_range(double x, double y, double step) {
    if (step > 0) {
        double a = x;
        while (a < y) {
            print(a);
            a = a + step;
        }
    }
}

# double x = read();
# double y = read();
# double step = read();

if(2 == 3 || 3 == 1)
{
print(1);
}

# print_number_range(x, y, step);